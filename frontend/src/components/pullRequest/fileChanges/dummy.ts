export const prFiles = [
  {
    fileId: "a109f76d4b7d7f71f4283fce95cb02f8a5ee4b07",
    commitId: "ba1aa52b2c619ba3d55b384bdc8c2c61909ac4d8",
    repoId: 888788,
    prId: 33,
    fileName: "index.tsx",
    oldPath: "frontend/src/components/pullRequest/fileChanges/index.tsx",
    newPath: "frontend/src/components/pullRequest/fileChanges/index.tsx",
    fileStatus: 2,
    targetBranch: "develop",
    sourceBranch: "FE/feature/MarkDownEditor",
  },
];

export const file = {
  fileName: "index.tsx",
  oldCode:
    'import { SimpleTreeView, TreeItem } from "@mui/x-tree-view";\nimport { DiffViewer } from "../diffViewer";\nimport { Box, Accordion, AccordionSummary } from "@mui/material";\nimport AccordionDetails from "@mui/material/AccordionDetails";\nimport ArrowDropDownIcon from "@mui/icons-material/ArrowDropDown";\nimport { ErrorBoundery } from "../../common/errorBoundery";\nconst files = [\n  {\n    filePath: "1",\n    oldCode:\n      \'package com.giteat.common.config;\\n\\nimport org.springframework.context.annotation.Bean;\\nimport org.springframework.context.annotation.Configuration;\\nimport org.springframework.context.annotation.Profile;\\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\\nimport static org.springframework.security.config.Customizer.withDefaults;\\nimport org.springframework.security.web.SecurityFilterChain;\\nimport org.springframework.security.web.util.matcher.RequestMatcher;\\nimport jakarta.servlet.http.HttpServletRequest;\\n\\n@Profile("release") // 배포 환경에서만 활성화\\n@Configuration\\npublic class ReleaseSecurityConfig {\\n    private static final String ALLOWED_IP = "192.168.99.100"; // 허용된 IP 주소\\n\\n    @Bean\\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\\n        http.csrf(withDefaults()) // CSRF 기본 설정 유지\\n                .authorizeHttpRequests(auth -> auth\\n                        .requestMatchers(new RequestMatcher() {\\n                            @Override\\n                            public boolean matches(HttpServletRequest request) {\\n                                String remoteAddr = request.getRemoteAddr();\\n                                return ALLOWED_IP.equals(remoteAddr);\\n                            }\\n                        }).permitAll() // 허용된 IP에서만 요청 허용\\n                        .anyRequest().denyAll()); // 나머지 요청 차단\\n        return http.build();\\n    }\\n}\\n\',\n    diff: "현재는 임시값",\n    newCode:\n      \'package com.giteat.common.config;\\n\\nimport org.springframework.context.annotation.Bean;\\nimport org.springframework.context.annotation.Configuration;\\nimport org.springframework.context.annotation.Profile;\\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\\nimport static org.springframework.security.config.Customizer.withDefaults;\\nimport org.springframework.security.web.SecurityFilterChain;\\nimport org.springframework.security.web.util.matcher.RequestMatcher;\\nimport jakarta.servlet.http.HttpServletRequest;\\n\\n@Profile("release") // 배포 환경에서만 활성화\\n@Configuration\\npublic class ReleaseSecurityConfig {\\n    private static final String ALLOWED_IP = "192.168.99.100"; // 허용된 IP 주소\\n\\n    @Bean\\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\\n        http.csrf(withDefaults()) // CSRF 기본 설정 유지\\n                .authorizeHttpRequests(auth -> auth\\n                        .requestMatchers(new RequestMatcher() {\\n                            @Override\\n                            public boolean matches(HttpServletRequest request) {\\n                                String remoteAddr = request.getRemoteAddr();\\n                                return ALLOWED_IP.equals(remoteAddr);\\n                            }\\n                        }).permitAll() // 허용된 IP에서만 요청 허용\\n                        .anyRequest().denyAll()); // 나머지 요청 차단\\n        return http.build();\\n    }\\n}\\n\',\n  },\n  {\n    filePath: "2",\n    oldCode:\n      \'ackage com.giteat.common.config;\\n\\nimport org.springframework.context.annotation.Bean;\\nimport org.springframework.context.annotation.Configuration;\\nimport org.springframework.context.annotation.Profile;\\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\\nimport static org.springframework.security.config.Customizer.withDefaults;\\nimport org.springframework.security.web.SecurityFilterChain;\\nimport org.springframework.security.web.util.matcher.RequestMatcher;\\nimport jakarta.servlet.http.HttpServletRequest;\\n\\n@Profile("release") // 배포 환경에서만 활성화\\n@Configuration\\npublic class ReleaseSecurityConfig {\\n    private static final String ALLOWED_IP = "192.168.99.99"; // 허용된 IP 주소\\n\\n    @Bean\\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\\n        http.csrf(withDefaults()) // CSRF 기본 설정 유지\\n                .authorizeHttpRequests(auth -> auth\\n                        .requestMatchers(new RequestMatcher() {\\n                            @Override\\n                            public boolean matches(HttpServletRequest request) {\\n                                String remoteAddr = request.getRemoteAddr();\\n                                return ALLOWED_IP.equals(remoteAddr);\\n                            }\\n                        }).permitAll() // 허용된 IP에서만 요청 허용\\n                        .anyRequest().denyAll()); // 나머지 요청 차단\\n        return http.build();\\n    }\\n}\\n\',\n    diff: "현재는 임시값",\n    newCode:\n      \'package com.giteat.common.config;\\n\\nimport org.springframework.context.annotation.Bean;\\nimport org.springframework.context.annotation.Configuration;\\nimport org.springframework.context.annotation.Profile;\\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\\nimport static org.springframework.security.config.Customizer.withDefaults;\\nimport org.springframework.security.web.SecurityFilterChain;\\nimport org.springframework.security.web.util.matcher.RequestMatcher;\\nimport jakarta.servlet.http.HttpServletRequest;\\n\\n@Profile("release") // 배포 환경에서만 활성화\\n@Configuration\\npublic class ReleaseSecurityConfig {\\n    private static final String ALLOWED_IP = "192.168.99.99"; // 허용된 IP 주소\\n\\n    @Bean\\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\\n        http.csrf(withDefaults()) // CSRF 기본 설정 유지\\n                .authorizeHttpRequests(auth -> auth\\n                        .requestMatchers(new RequestMatcher() {\\n                            @Override\\n                            public boolean matches(HttpServletRequest request) {\\n                                String remoteAddr = request.getRemoteAddr();\\n                                return ALLOWED_IP.equals(remoteAddr);\\n                            }\\n                        }).permitAll() // 허용된 IP에서만 요청 허용\\n                        .anyRequest().denyAll()); // 나머지 요청 차단\\n        return http.build();\\n    }\\n}\\n\',\n  },\n  {\n    filePath: "3",\n    oldCode:\n      \'package com.giteat.common.config;\\n\\nimport org.springframework.context.annotation.Bean;\\nimport org.springframework.context.annotation.Configuration;\\nimport org.springframework.context.annotation.Profile;\\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\\nimport static org.springframework.security.config.Customizer.withDefaults;\\nimport org.springframework.security.web.SecurityFilterChain;\\nimport org.springframework.security.web.util.matcher.RequestMatcher;\\nimport jakarta.servlet.http.HttpServletRequest;\\n\\n@Profile("release") // 배포 환경에서만 활성화\\n@Configuration\\npublic class ReleaseSecurityConfig {\\n    private static final String ALLOWED_IP = "192.168.99.100"; // 허용된 IP 주소\\n\\n    @Bean\\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\\n        http.csrf(withDefaults()) // CSRF 기본 설정 유지\\n                .authorizeHttpRequests(auth -> auth\\n                        .requestMatchers(new RequestMatcher() {\\n                            @Override\\n                            public boolean matches(HttpServletRequest request) {\\n                                String remoteAddr = request.getRemoteAddr();\\n                                return ALLOWED_IP.equals(remoteAddr);\\n                            }\\n                        }).permitAll() // 허용된 IP에서만 요청 허용\\n                        .anyRequest().denyAll()); // 나머지 요청 차단\\n        return http.build();\\n    }\\n}\\n\',\n    diff: "현재는 임시값",\n    newCode:\n      \'ackage com.giteat.common.config;\\n\\nimport org.springframework.context.annotation.Bean;\\nimport org.springframework.context.annotation.Configuration;\\nimport org.springframework.context.annotation.Profile;\\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\\nimport static org.springframework.security.config.Customizer.withDefaults;\\nimport org.springframework.security.web.SecurityFilterChain;\\nimport org.springframework.security.web.util.matcher.RequestMatcher;\\nimport jakarta.servlet.http.HttpServletRequest;\\n\\n@Profile("release") // 배포 환경에서만 활성화\\n@Configuration\\npublic class ReleaseSecurityConfig {\\n    private static final String ALLOWED_IP = "192.168.99.100"; // 허용된 IP 주소\\n\\n    @Bean\\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\\n        http.csrf(withDefaults()) // CSRF 기본 설정 유지\\n                .authorizeHttpRequests(auth -> auth\\n                        .requestMatchers(new RequestMatcher() {\\n                            @Override\\n                            public boolean matches(HttpServletRequest request) {\\n                                String remoteAddr = request.getRemoteAddr();\\n                                return ALLOWED_IP.equals(remoteAddr);\\n                            }\\n                        }).permitAll() // 허용된 IP에서만 요청 허용\\n                        .anyRequest().denyAll()); // 나머지 요청 차단\\n        return http.build();\\n    }\\n}\\n\',\n  },\n];\n\nexport function FileChanges() {\n  return (\n    <div className="flex gap-[35px] justify-between mt-[30px]">\n      <div className="w-1/5 max-w-56 bg-white p-[15px] min-h-[calc(100vh-300px)] max-h-[calc(100vh-300px)] rounded-xl">\n        <Box>\n          <SimpleTreeView>\n            <TreeItem itemId="grid" label="Data Grid">\n              <TreeItem itemId="grid-community" label="@mui/x-data-grid" />\n              <TreeItem itemId="grid-pro" label="@mui/x-data-grid-pro" />\n              <TreeItem\n                itemId="grid-premium"\n                label="@mui/x-data-grid-premium"\n              />\n            </TreeItem>\n            <TreeItem itemId="pickers" label="Date and Time Pickers">\n              <TreeItem\n                itemId="pickers-community"\n                label="@mui/x-date-pickers"\n              />\n              <TreeItem itemId="pickers-pro" label="@mui/x-date-pickers-pro" />\n            </TreeItem>\n            <TreeItem itemId="charts" label="Charts">\n              <TreeItem itemId="charts-community" label="@mui/x-charts" />\n            </TreeItem>\n            <TreeItem itemId="tree-view" label="Tree View">\n              <TreeItem itemId="tree-view-community" label="@mui/x-tree-view" />\n            </TreeItem>\n          </SimpleTreeView>\n        </Box>\n      </div>\n      <div className="w-fit">\n        <div className="mt-4">\n          <div className="border border-gray-200 p-4 my-4 rounded-md">\n            {files.map((file) => (\n              <Accordion key={file.filePath} defaultExpanded>\n                <div className="flex justify-between items-center">\n                  <AccordionSummary\n                    expandIcon={<ArrowDropDownIcon />}\n                    aria-controls="panel1-content"\n                    id="panel1-header"\n                  >\n                    <h2 className="text-lg font-bold">TEST.java</h2>\n                  </AccordionSummary>\n                </div>\n                <div className="flex mt-4">\n                  <AccordionDetails>\n                    <ErrorBoundery>\n                      <DiffViewer\n                        oldCode={file.oldCode}\n                        newCode={file.newCode}\n                      />\n                    </ErrorBoundery>\n                  </AccordionDetails>\n                </div>\n              </Accordion>\n            ))}\n          </div>\n        </div>\n      </div>\n    </div>\n  );\n}\n',
  newCode:
    'import { SimpleTreeView, TreeItem } from "@mui/x-tree-view";\nimport { DiffViewer } from "../diffViewer";\nimport { Box, Accordion, AccordionSummary } from "@mui/material";\nimport AccordionDetails from "@mui/material/AccordionDetails";\nimport ArrowDropDownIcon from "@mui/icons-material/ArrowDropDown";\nimport { ErrorBoundery } from "../../common/errorBoundery";\n\nconst files = [\n  {\n    fileName: "1",\n    oldCode:\n      \'ackage com.giteat.common.config;\\n\\nackage com.giteat.common.config;\\n\\nimport org.springframework.context.annotation.Bean;\\nimport org.springframework.context.annotation.Configuration;\\nimport org.springframework.context.annotation.Profile;\\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\\nimport static org.springframework.security.config.Customizer.withDefaults;\\nimport org.springframework.security.web.SecurityFilterChain;\\nimport org.springframework.security.web.util.matcher.RequestMatcher;\\nimport jakarta.servlet.http.HttpServletRequest;\\n\\n@Profile("release") // 배포 환경에서만 활성화\\n@Configuration\\npublic class ReleaseSecurityConfig {\\n    private static final String ALLOWED_IP = "192.168.99.99"; // 허용된 IP 주소\\n\\n    @Bean\\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\\n        http.csrf(withDefaults()) // CSRF 기본 설정 유지\\n                .authorizeHttpRequests(auth -> auth\\n                        .requestMatchers(new RequestMatcher() {\\n                            @Override\\n                            public boolean matches(HttpServletRequest request) {\\n                                String remoteAddr = request.getRemoteAddr();\\n                                return ALLOWED_IP.equals(remoteAddr);\\n                            }\\n                        }).permitAll() // 허용된 IP에서만 요청 허용\\n                        .anyRequest().denyAll()); // 나머지 요청 차단\\n        return http.build();\\n    }\\n}\\n\',\n    diff: "현재는 임시값",\n    newCode:\n      \'package com.giteat.common.config;\\n\\nackage com.giteat.common.config;\\n\\n3ackage com.giteat.common.config;\\n\\nimport org.springframework.context.annotation.Bean;\\nimport org.springframework.context.annotation.Configuration;\\nimport org.springframework.context.annotation.Profile;\\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\\nimport static org.springframework.security.config.Customizer.withDefaults;\\nimport org.springframework.security.web.SecurityFilterChain;\\nimport org.springframework.security.web.util.matcher.RequestMatcher;\\nimport jakarta.servlet.http.HttpServletRequest;\\n\\n@Profile("release") // 배포 환경에서만 활성화\\n@Configuration\\npublic class ReleaseSecurityConfig {\\n    private static final String ALLOWED_IP = "192.168.99.99"; // 허용된 IP 주소\\n\\n    @Bean\\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\\n        http.csrf(withDefaults()) // CSRF 기본 설정 유지\\n                .authorizeHttpRequests(auth -> auth\\n                        .requestMatchers(new RequestMatcher() {\\n                            @Override\\n                            public boolean matches(HttpServletRequest request) {\\n                                String remoteAddr = request.getRemoteAddr();\\n                                return ALLOWED_IP.equals(remoteAddr);\\n                            }\\n                        }).permitAll() // 허용된 IP에서만 요청 허용\\n                        .anyRequest().denyAll()); // 나머지 요청 차단\\n        return http.build();\\n    }\\n}\\n\',\n  },\n  {\n    fileName: "2",\n    oldCode:\n      \'ackage com.giteat.common.config;\\n\\nimport org.springframework.context.annotation.Bean;\\nimport org.springframework.context.annotation.Configuration;\\nimport org.springframework.context.annotation.Profile;\\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\\nimport static org.springframework.security.config.Customizer.withDefaults;\\nimport org.springframework.security.web.SecurityFilterChain;\\nimport org.springframework.security.web.util.matcher.RequestMatcher;\\nimport jakarta.servlet.http.HttpServletRequest;\\n\\n@Profile("release") // 배포 환경에서만 활성화\\n@Configuration\\npublic class ReleaseSecurityConfig {\\n    private static final String ALLOWED_IP = "192.168.99.99"; // 허용된 IP 주소\\n\\n    @Bean\\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\\n        http.csrf(withDefaults()) // CSRF 기본 설정 유지\\n                .authorizeHttpRequests(auth -> auth\\n                        .requestMatchers(new RequestMatcher() {\\n                            @Override\\n                            public boolean matches(HttpServletRequest request) {\\n                                String remoteAddr = request.getRemoteAddr();\\n                                return ALLOWED_IP.equals(remoteAddr);\\n                            }\\n                        }).permitAll() // 허용된 IP에서만 요청 허용\\n                        .anyRequest().denyAll()); // 나머지 요청 차단\\n        return http.build();\\n    }\\n}\\n\',\n    diff: "현재는 임시값",\n    newCode:\n      \'package com.giteat.common.config;\\n\\nimport org.springframework.context.annotation.Bean;\\nimport org.springframework.context.annotation.Configuration;\\nimport org.springframework.context.annotation.Profile;\\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\\nimport static org.springframework.security.config.Customizer.withDefaults;\\nimport org.springframework.security.web.SecurityFilterChain;\\nimport org.springframework.security.web.util.matcher.RequestMatcher;\\nimport jakarta.servlet.http.HttpServletRequest;\\n\\n@Profile("release") // 배포 환경에서만 활성화\\n@Configuration\\npublic class ReleaseSecurityConfig {\\n    private static final String ALLOWED_IP = "192.168.99.99"; // 허용된 IP 주소\\n\\n    @Bean\\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\\n        http.csrf(withDefaults()) // CSRF 기본 설정 유지\\n                .authorizeHttpRequests(auth -> auth\\n                        .requestMatchers(new RequestMatcher() {\\n                            @Override\\n                            public boolean matches(HttpServletRequest request) {\\n                                String remoteAddr = request.getRemoteAddr();\\n                                return ALLOWED_IP.equals(remoteAddr);\\n                            }\\n                        }).permitAll() // 허용된 IP에서만 요청 허용\\n                        .anyRequest().denyAll()); // 나머지 요청 차단\\n        return http.build();\\n    }\\n}\\n\',\n  },\n  {\n    fileName: "3",\n    oldCode:\n      \'package com.giteat.common.config;\\n\\nimport org.springframework.context.annotation.Bean;\\nimport org.springframework.context.annotation.Configuration;\\nimport org.springframework.context.annotation.Profile;\\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\\nimport static org.springframework.security.config.Customizer.withDefaults;\\nimport org.springframework.security.web.SecurityFilterChain;\\nimport org.springframework.security.web.util.matcher.RequestMatcher;\\nimport jakarta.servlet.http.HttpServletRequest;\\n\\n@Profile("release") // 배포 환경에서만 활성화\\n@Configuration\\npublic class ReleaseSecurityConfig {\\n    private static final String ALLOWED_IP = "192.168.99.100"; // 허용된 IP 주소\\n\\n    @Bean\\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\\n        http.csrf(withDefaults()) // CSRF 기본 설정 유지\\n                .authorizeHttpRequests(auth -> auth\\n                        .requestMatchers(new RequestMatcher() {\\n                            @Override\\n                            public boolean matches(HttpServletRequest request) {\\n                                String remoteAddr = request.getRemoteAddr();\\n                                return ALLOWED_IP.equals(remoteAddr);\\n                            }\\n                        }).permitAll() // 허용된 IP에서만 요청 허용\\n                        .anyRequest().denyAll()); // 나머지 요청 차단\\n        return http.build();\\n    }\\n}\\n\',\n    diff: "현재는 임시값",\n    newCode:\n      \'ackage com.giteat.common.config;\\n\\nimport org.springframework.context.annotation.Bean;\\nimport org.springframework.context.annotation.Configuration;\\nimport org.springframework.context.annotation.Profile;\\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\\nimport static org.springframework.security.config.Customizer.withDefaults;\\nimport org.springframework.security.web.SecurityFilterChain;\\nimport org.springframework.security.web.util.matcher.RequestMatcher;\\nimport jakarta.servlet.http.HttpServletRequest;\\n\\n@Profile("release") // 배포 환경에서만 활성화\\n@Configuration\\npublic class ReleaseSecurityConfig {\\n    private static final String ALLOWED_IP = "192.168.99.100"; // 허용된 IP 주소\\n\\n    @Bean\\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\\n        http.csrf(withDefaults()) // CSRF 기본 설정 유지\\n                .authorizeHttpRequests(auth -> auth\\n                        .requestMatchers(new RequestMatcher() {\\n                            @Override\\n                            public boolean matches(HttpServletRequest request) {\\n                                String remoteAddr = request.getRemoteAddr();\\n                                return ALLOWED_IP.equals(remoteAddr);\\n                            }\\n                        }).permitAll() // 허용된 IP에서만 요청 허용\\n                        .anyRequest().denyAll()); // 나머지 요청 차단\\n        return http.build();\\n    }\\n}\\n\',\n  },\n];\n\nexport function FileChanges() {\n  return (\n    <div className="flex gap-[35px] justify-between mt-[30px]">\n      <div className="w-1/5 max-w-56 bg-white p-[15px] min-h-[calc(100vh-300px)] max-h-[calc(100vh-300px)] rounded-xl">\n        <Box>\n          <SimpleTreeView>\n            <TreeItem itemId="grid" label="Data Grid">\n              <TreeItem itemId="grid-community" label="@mui/x-data-grid" />\n              <TreeItem itemId="grid-pro" label="@mui/x-data-grid-pro" />\n              <TreeItem\n                itemId="grid-premium"\n                label="@mui/x-data-grid-premium"\n              />\n            </TreeItem>\n            <TreeItem itemId="pickers" label="Date and Time Pickers">\n              <TreeItem\n                itemId="pickers-community"\n                label="@mui/x-date-pickers"\n              />\n              <TreeItem itemId="pickers-pro" label="@mui/x-date-pickers-pro" />\n            </TreeItem>\n            <TreeItem itemId="charts" label="Charts">\n              <TreeItem itemId="charts-community" label="@mui/x-charts" />\n            </TreeItem>\n            <TreeItem itemId="tree-view" label="Tree View">\n              <TreeItem itemId="tree-view-community" label="@mui/x-tree-view" />\n            </TreeItem>\n          </SimpleTreeView>\n        </Box>\n      </div>\n      <div className="w-fit">\n        <div className="mt-4">\n          <div className="border border-gray-200 p-4 my-4 rounded-md">\n            {files.map((file) => (\n              <Accordion key={file.fileName} defaultExpanded>\n                <div className="flex justify-between items-center">\n                  <AccordionSummary\n                    expandIcon={<ArrowDropDownIcon />}\n                    aria-controls="panel1-content"\n                    id="panel1-header"\n                  >\n                    <h2 className="text-lg font-bold">TEST.java</h2>\n                  </AccordionSummary>\n                </div>\n                <div className="flex mt-4">\n                  <AccordionDetails>\n                    <ErrorBoundery>\n                      <DiffViewer\n                        oldCode={file.oldCode}\n                        newCode={file.newCode}\n                      />\n                    </ErrorBoundery>\n                  </AccordionDetails>\n                </div>\n              </Accordion>\n            ))}\n          </div>\n        </div>\n      </div>\n    </div>\n  );\n}\n',
  comments: [
    {
      body: {
        content: "잘했음",
      },
      position: {
        base_sha: "46edebc6b129916f4e0f2b485add074ccb8f385a",
        start_sha: "46edebc6b129916f4e0f2b485add074ccb8f385a",
        head_sha: "3d40bc144d918d1eb890c7a373c6072cb812cdd4",
        old_path: "backend/rest/src/main/java/com/giteat/api/GitLabApi.java",
        new_path: "backend/rest/src/main/java/com/giteat/api/GitLabApi.java",
        position_type: "text",
        old_line: 19,
        new_line: 20,
        line_range: {
          start: {
            line_code: "a109f76d4b7d7f71f4283fce95cb02f8a5ee4b07_9_9",
          },
          end: {
            line_code: "524cec10819d109f27582f3dd600dc7a0ef4d500_19_20",
          },
        },
      },
    },
    {
      body: {
        content: "못했음음",
      },
      position: {
        base_sha: "46edebc6b129916f4e0f2b485add074ccb8f385a",
        start_sha: "46edebc6b129916f4e0f2b485add074ccb8f385a",
        head_sha: "3d40bc144d918d1eb890c7a373c6072cb812cdd4",
        old_path: "backend/rest/src/main/java/com/giteat/api/GitLabApi.java",
        new_path: "backend/rest/src/main/java/com/giteat/api/GitLabApi.java",
        position_type: "text",
        new_line: 7,
        line_range: {
          start: {
            line_code: "a109f76d4b7d7f71f4283fce95cb02f8a5ee4b07_null_7",
          },
          end: {
            line_code: "524cec10819d109f27582f3dd600dc7a0ef4d500_null_7",
          },
        },
      },
    },
    {
      body: {
        content: "평범함",
      },
      position: {
        base_sha: "46edebc6b129916f4e0f2b485add074ccb8f385a",
        start_sha: "46edebc6b129916f4e0f2b485add074ccb8f385a",
        head_sha: "3d40bc144d918d1eb890c7a373c6072cb812cdd4",
        old_path: "backend/rest/src/main/java/com/giteat/api/GitLabApi.java",
        new_path: "backend/rest/src/main/java/com/giteat/api/GitLabApi.java",
        position_type: "text",
        old_line: 9,
        line_range: {
          start: {
            line_code: "a109f76d4b7d7f71f4283fce95cb02f8a5ee4b07_9_null",
          },
          end: {
            line_code: "524cec10819d109f27582f3dd600dc7a0ef4d500_9_null",
          },
        },
      },
    },
  ],
};
